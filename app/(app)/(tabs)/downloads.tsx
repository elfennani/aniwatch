import React from "react";
import Text from "@/components/text";
import { useTheme } from "@/ctx/theme-provider";

const DownloadScreen = () => {
  const { spacing } = useTheme();

  return <Text>To Be Added</Text>;
};

export default DownloadScreen;
